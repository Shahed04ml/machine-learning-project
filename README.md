the workflow is preprossesing->train->evaluation->visualization
the models used are random forest, logistic regression
then for the api it will show the prediction for values u put for either logistic regression or random forest just open (http://127.0.0.1:8000/docs#/default/predict_predict_post), after running the program
