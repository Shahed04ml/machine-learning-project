the workflow is preprossesing->train->evaluation->visualization->api

1-the models used are random forest, logistic regression.

2-for the api it will show the prediction for values u put for either logistic regression(lr) or random forest(rf) just open (http://127.0.0.1:8000/docs), after running the program especially the api.
